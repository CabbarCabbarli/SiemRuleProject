title: "Registry Persistence via Run Keys"
id: "rule-05"
description: "Registry Run key-lərinə dəyişiklik."
detection:
  condition: |
    index=windows EventCode=13 TargetObject IN ("*\\Run\\*", "*\\RunOnce\\*")
